**constants.ts**
→ chứa URL, viewport, storage path, timeout, fixed config

**browser.ts**
→ tạo browser/context/page

**airports.ts**
→ thao tác chọn airport

**waiters.ts**
→ các hàm đợi UI/data/loading

**screenshots.ts**
→ xử lý screenshot/file path

**flight-search.ts**
→ flow nghiệp vụ search flight

**flight-selection.ts**
→ flow chọn chuyến

**hold-booking.ts**
→ flow giữ chỗ

**pnr.ts**
→ extract/validate mã PNR